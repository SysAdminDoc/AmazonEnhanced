# Node / build
node_modules/
dist/
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~

# Extension packaging — release artifacts, not tracked in-tree
*.zip
*.crx
*.pem

# Signing key — NEVER commit. Losing this changes the extension ID.
build/amazonenhanced.pem

# MHTML reference snapshots + debug screenshots
*.mhtml
1.png
2.png
3.png

# Local private settings
local-settings.json
CLAUDE.md
